{
   "name": "@duplocode/attestation",
   "version": "0.0.0",
   "private": true,
   "type": "module",
   "description": "Injectable signer/verifier boundary for signed PublicationDecision attestations. Binds a canonical payload to a trusted control-plane issuer; verification fails closed on unsigned, mismatched, or untrusted attestations. Signing is a control-plane concern and is not yet wired into governance.",
   "exports": "./dist/index.js",
   "types": "./dist/index.d.ts",
   "scripts": {
      "build": "tsc -p tsconfig.json",
      "typecheck": "tsc -p tsconfig.json --noEmit",
      "test": "node --test dist/**/*.test.js"
   },
   "dependencies": {
      "@duplocode/composition": "workspace:*",
      "@duplocode/contracts": "workspace:*"
   }
}
